fun main(vararg args: String) {
    /*
    - Read content of args[0] into list of strings
    - Create table
    - Print first page of table

    Content -> CsvParser -> Table -> TableView (pagination) -> ConsoleRenderer
    UserInterface (Table, UserInput -> TableView -> ConsoleRenderer)
    */
}
